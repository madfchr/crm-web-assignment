<h1>All Contacts</h1>

<ul>
<% Contact.all.each do |contact| %>
  <li>
    <a href="contacts/<%= contact.id %>"><%= contact.first_name %> <%= contact.last_name %>
  </li>
<% end %>
</ul>

<!-- # I could add more details here, for example, output the contact's email or note -->
